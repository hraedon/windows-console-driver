# Claim registry

Bookkeeping for the independence discipline: each evidence claim this project
pursues maps to the observers that can certify it and the independence
relationship between them. "Limit the claim to what the second oracle actually
proves" is enforced here, in review, not in folklore.

These rows register measured claims, not blanket qualification of whichever
capability revision happens to be current. The explicit current-revision matrix
is in `docs/capability-qualification.md`.

| Claim | Certifying observer(s) | Category | Independence notes | Certified |
|---|---|---|---|---|
| GPMC (Server 2025) writes `scripts.ini` as UTF-16LE BOM + CRLF, split-style entries, empty `1Parameters=` retained | Oracle read via PSDirect (`oracle-read.ps1`), bytes + decoded | state | Observer is PowerShell Direct; actuation was helper UIA/keyboard via the console session — no shared code or channel | 2026-09-03 (R2 pilot) |
| `psscripts.ini` ordering is encoded as `[ScriptsConfig] StartExecutePSFirst=true`; no `[Policy]` section exists on the wire | Same oracle read | state | Same independence; the actuated dropdown ("Run Windows PowerShell scripts first") and the observed key are linked by one controlled transaction | 2026-09-03 (R2 pilot) |
| Machine version increments by exactly 1 in gpt.ini and AD `versionNumber` on this authoring transaction | Same oracle read (pre from R2 setup: new GPO at 0) | state | `derive` relation, not literal; both stores read independently | 2026-09-03 (R2 pilot) |
| R1: GPMC-authored migration tables use default namespace `http://www.microsoft.com/GroupPolicy/GPOOperations/MigrationTable` with `Mapping → Type/Source/Destination` child elements — NOT the `GroupPolicy/Types` namespace nor `Identifier → Sid/Name` shape `migration.py` assumes. A GPMC table parses to an **empty table, silently**, in `parse_migration_table` | `migtable` collector (controller-side XML parse of PSDirect-transported bytes); legacy record `runs/r1-migtable.json`; native-v1 re-certification `docs/estate-window-4/records/r1-v1-record.json`; raw artifact `runs/r1-studio.migtable` | state | COM authoring (GPMgmt.GPM on the guest) vs PSDirect byte transport vs Python parse — three independent channels | 2026-09-03 (R1 initial); 2026-09-04 (native v1 re-certification, both `verified`) |
| R1 (constants): GPM COM `EntryType*` on Server 2025 = User 0, Computer 1, LocalGroup 2, GlobalGroup 3, UniversalGroup 4, UNCPath 5, Unknown 6. No `EntryTypeDomainLocalGroup`. "Same as source" requires passing the source name as destination (empty string → E_INVALIDARG) | Same collector flow; measured in `runs/probe_addentry.py` | state | Same independence | 2026-09-03 (R1) |
| R2 (engine re-run): the R2 pilot's claims re-certified through the transactional engine — envelope `satisfied`, reproduce `2/2`, strict-absence cleanup `remaining=0`; GPMC registered Scripts CSEs `{42B5FAAE-6536-11D2-AE5A-0000F87571E3}{40B6664F-4972-11D1-A7CA-0000F87571E3}` in `gPCMachineExtensionNames` on first authoring | Full observer set over PSDirect; legacy record `docs/estate-window-2/records/r2-record.json`; revision-2 record `docs/estate-window-4/records/r2-v2-record.json` | state | Actuation helper UIA/keyboard; observers PSDirect; envelope engine separate | 2026-09-03 (revision 1); 2026-09-04 (revision 2, exact entry envelope, both `verified`) |
| R4: propagation codes in `GptTmpl.inf` are **propagate=0, do-not-allow-replace=1, replace=2** (measured per authored key: Alpha/Propagate→0, Charlie/Do-not-allow→1, Bravo/Replace→2). The spec-informed fixture was right; `object_security.py` `_propagation_from_code` (0=none, 1=propagate, 2=replace) is wrong on all three | `gpttmpl_inf` observer (`by_key` facts), legacy record `docs/estate-window-2/records/r4-record.json` delta; clean revision-2 state record `docs/estate-window-4/records/r4-v2-record.json`; trace-complete qualification record `docs/estate-window-4/records/r4-v2b-record.json` | state | Security Settings snap-in driven via helper; GptTmpl.inf bytes transported via PSDirect; parsed controller-side; the final run retained six distinct per-key screenshots | 2026-09-03 (revision-1 state verified, cleanup failed); 2026-09-04 (revision 2, `verified`, strict cleanup `remaining=0`; `r4-v2b` is the qualification artifact) |
| R4: native `GptTmpl.inf` is UTF-16LE BOM + CRLF-only, and `[Registry Keys]` entries are bare quoted-CSV rows (`"KEY",code,"SDDL"`) — `key = value` shape appears nowhere | Same observer, `encoding` + `shape` facts | state | Same independence | 2026-09-03 (R4) |
| R5: `gpt.ini`/AD `versionNumber` is the packed 32-bit field `user*65536 + machine`, maintained as halves: a machine-only Registry-CSE edit moved 0→1; a user-only edit moved 0→65536 with the machine half untouched. `publication.py`'s flat `+1` on a user-side change would corrupt the field | `version_values` + `ad_attributes` observers; the derive relation `post.version.ad_versionNumber == post.version.user * 65536 + post.version.machine` is asserted in both R5 capability envelopes; legacy records `docs/estate-window-2/records/r5m-record.json`, `docs/estate-window-2/records/r5u-record.json`; revision-2 records `docs/estate-window-4/records/r5m-v2-record.json`, `docs/estate-window-4/records/r5u-v2-record.json` | state | Registry-CSE authoring via helper; AD+SYSVOL read via PSDirect; packing relation and side-specific `Registry.pol` path asserted by the bounded evaluator over independently observed pre/post | 2026-09-03 (revision 1); 2026-09-04 (revision 2, both `verified`, strict cleanup `remaining=0`) |
| R5: the user-side authoring also created `User\Registry.pol` (172 B) and `User\comment.cmtx` (553 B) — GPME writes comment sidecar files on authoring | `sysvol_tree_fingerprint` two-pass comparison | state | Same independence | 2026-09-03 (R5 user) |
| R9: `secedit /validate` **rejects** `object_security.py`'s `key = value` shape ("The data is invalid. Object 2 must have 3 fields each line.", exit 1) and **accepts** the native bare quoted-CSV shape (exit 0, "Template ... is validated") | `secedit` itself, on LabMS01; builder `gpo-studio/scripts/plan-033/build-object-security-candidate.py`; both candidate files required a `[Version] signature="$CHICAGO$" Revision=1` preamble to reach row-level validation | behavior | The oracle is Windows' own parser; the two candidates differ only in row shape | 2026-09-03 (R9) |
| R3: a Basic Documents Folder Redirection policy writes an empty UTF-16LE/CRLF `User/Documents & Settings/fdeploy.ini` marker and the semantic policy in `fdeploy1.ini`: three sections, four entries, `version=100`, `Flags=1021`, and `FullPath=\\zz-studio-fileserver\zzredir\%USERNAME%\Documents`; the user version half moves 0→1 and packed AD/SYSVOL version 0→65536 | `fdeploy_marker` + `fdeploy` collectors over PSDirect-transported bytes, AD attributes, and packed version observer; record `docs/estate-window-3/records/r3-window3-record.json` | state | Folder Redirection UI actuated through targeted helper input; SYSVOL and AD observed independently over PSDirect; the bounded envelope checked exact shape and version relations | 2026-09-04 (R3, `verified`, strict cleanup `remaining=0`) |
| R2 (PowerShell-order qualification): the visible GPMC selection “Run Windows PowerShell scripts first” produces `[ScriptsConfig] StartExecutePSFirst=true` while preserving exactly two command-script entries and one PowerShell entry, including the empty second parameter field | `scripts_ini` / `psscripts_ini` observers over PSDirect; targeted before/after UI dump and screenshot; record `docs/estate-window-3/records/r2-psorder-window3-record.json` | state | The order control was actuated in the console UI; file bytes and AD/SYSVOL version deltas were observed through the independent oracle; exact entry counts and values were envelope requirements | 2026-09-04 (R2 ps-order, `verified`, strict cleanup `remaining=0`) |
| R10: Windows **accepts** a Studio-written `scripts.ini`/`psscripts.ini` backup: `Import-GPO` consumed the Studio bundle, registered the measured Scripts CSE pair `[{42B5FAAE-6536-11D2-AE5A-0000F87571E3}{40B6664F-4972-11D1-A7CA-0000F87571E3}]` in `gPCMachineExtensionNames`, and `Backup-GPO` re-emitted **both INIs byte-identical** to Studio's output (itself byte-identical to the R2 GPMC captures); GPME renders both command entries, the PowerShell entry with `-Mode Alpha`, and the ordering dropdown at "Run Windows PowerShell scripts first" | Windows' own importer/re-exporter on LabMS01; record `docs/estate-window-5/records/r10-record.json`; screenshots `runs/r10-startup-{scripts,psscripts}-tab.png` hash-bound in the record; bundle pinned to gpo-studio commit `38e3e9f` | behavior + state | Import and re-export ran over PSDirect (controller-driven); render observed through the console-session editor (UIA dumps + screenshots); byte comparison done controller-side against the deterministic bundle | 2026-09-05 (R10, strict cleanup `remaining=0`) |
| R11: a **production directory accepts Studio's output exactly as the lab does**. `Import-GPO -CreateIfNeeded` into `ad.hraedon.com` consumed the deterministic WP-2 candidate bundle (SHA-256 `ddd72fd9…b0f3`): the GPC was created **unlinked** (LinksTo empty, 0 links), Windows preserved Studio's exact extension lists machine and user side — `[{35378EAC-683F-11D2-A89A-00C04FBBCFA2}{D02B1F72-…}]` / `[{35378EAC-…}{D02B1F73-…}]`, the user-side tool GUID never before observed in the wild (absent from the R6 census, present in `export.py`'s emission table) — packed `versionNumber=65537` (= user·65536+machine, matching the bundle), `gPCFunctionalityVersion=2`, `flags=0`, and `Backup-GPO` re-exported **both `registry.pol` files byte-identical** | The production directory itself (multi-DC, live); import/re-export via WinRM to mvmdc01 as the operator's domain-admin service account; byte comparison controller-side against the deterministic bundle | behavior + state | The oracle is the production forest; only semantic facts recorded (extension lists, integers, booleans, hashes) — the raw rebackup was deleted untranscribed. Operational lessons: unqualified GP cmdlets default to the LOGON account's domain context, so always pass `-Domain`/`-Server` explicitly; the first `Get-ADObject` after import can lose a multi-DC replication race (retry loop measured working) | 2026-09-05 (R11, strict cleanup `remaining=0`, with the operator's explicit go-ahead) |
| R7: the first `[Kerberos Policy]` section ever read from a real DC. The effective export is **MaxTicketAge=10, MaxRenewAge=7, MaxServiceAge=600, MaxClockSkew=5, TicketValidateClient=1** — the Windows defaults — and MaxTicketAge (**hours**) vs MaxServiceAge (**minutes**) are the two documented defaults for the SAME 10-hour duration, pinning the mixed units. `policy_families.py`'s three existing unit mappings (hours/days/minutes) are **correct as written**; the measured gap was the two unmodeled keys, now added (gpo-studio commit `055e5f5`). The AD domain object carries NO Kerberos override (attributes absent) — `secedit /export` was the only reporting surface, which is why this needed a DC. The sensitive full export was sliced on-box and deleted (`staging_removed=True`); only the five-key slice and unit facts left the machine | `secedit /export /areas SECURITYPOLICY` on mvmdc01 via WinRM as the operator's domain-admin service account (with the operator's go-ahead); slice + raw-attribute reads through the same session | state | The oracle is the DC's own effective-policy export; the committed ground truth is the five-key default slice (values are Windows defaults, committable per the work order) | 2026-09-05 (R7 — the work order's final request) |
| Revision-2 `setup.author_migration_table` bounds its own blast radius: the `GPMgmt.GPM` COM save writes the migration table and **nothing else** — `unexpected_entry_count` in the containing directory is 0 on Server 2025 | `migration_table` observer's parent-directory count (`Split-Path -Parent` of `migtable_path`, every entry but the table itself); record `docs/estate-window-6/records/r1-v2-record.json` | behavior | COM authoring on the guest vs PSDirect byte transport vs controller-side parse and predicate evaluation — the same three channels as the revision-1 row | 2026-09-05 (revision 2, `verified`, envelope satisfied, converged 2/2, cleanup `removed_dir`) |
| Estate ops: workstation lock/unlock auditing (4800/4801) shows **zero** 4800 events since auditing was armed (2026-09-03 ~01:00Z) and one 4801 manual unlock — the re-lock pattern predates the `InactivityTimeoutSecs=0` fix; no autonomous re-lock since | Security event log via PSDirect (`wcd ensure-console --audit`) | state | Event log read independent of all actuation | 2026-09-03 (second estate window) |
| R6: the extension-list census over all **26 production GPOs** of `ad.hraedon.com` (13 machine lists, 13 user lists, 3 GPOs with neither, 33 distinct CSE/tool GUIDs). **The two GPP XML root-element clsids `{3125E937-EB16-4b4c-9934-544FC6D24D26}` ("GPP Groups") and `{A3CC7818-8A30-4e0c-91C5-A4EA4B5A8DAB}` ("GPP Registry") — once two of `gpmc_interop._KNOWN_CSE_GUIDS`' three entries — appear in NO extension list: 0 of 26 GPOs, machine or user side** (verified case-insensitively over the raw CSV). The seventeen-backup sweep's hypothesis is confirmed against the production population; `EMITTED_EXTENSION_GUIDS`, built from what Windows actually registers, is the right basis for the known-CSE set | Read-only RSAT PowerShell census (Get-ADObject + extension-list extraction) from the operator's non-domain-admin account on mvmcc03; execution notes `docs/live-census-r6-r8/NOTES.md`; the census CSV is committed hash-bound at gpo-studio `tests/fixtures/live-domain-census/r06-cse-census/extension-lists.csv` (SHA-256 `bf10a400…3f9d`) | state | No engine, no transaction, no actuation: pure directory reads; sanitised by construction (GUID pairs only — no names, paths, or values); the production directory itself is the oracle | 2026-09-02 (R6); committed and hash-verified 2026-09-06 |
| R8: anatomy of three production GPOs (policy-heavy gpo1, GPP-Groups-both-sides gpo2, never-edited gpo3) plus the SYSVOL↔AD reconciliation over the whole directory: 30 GPC-named folders ↔ 29 DN-addressable objects ↔ **26 `groupPolicyContainer`** objects; one orphaned SYSVOL folder with no AD object; three `{GUID}`-CN objects in Policies that are NOT class `groupPolicyContainer` (predating the production regime). **`gpos_with_wmi_filter` = 3** — production confirmation that `associate_wmi_filter` models a real association. **gpo2's `GPT.INI` `Version=131082` (`0x0002000A`) = user 2 × 65536 + machine 10** — live corroboration of R5's packed-halves arithmetic; gpo3 shows `Version=0` with a populated SYSVOL folder (created, never edited). Methodology finding: PS 5.1 Guid-object string comparisons and lowercase-GUID `Test-Path` behaved inconsistently here — folder-name/DN joins are the reliable join, and any scripted census should avoid Guid-object string comparison entirely | Same read-only census session as R6 (RSAT PowerShell from mvmcc03, operator's non-domain-admin account); artifacts committed hash-bound at gpo-studio `tests/fixtures/live-domain-census/r08-gpo-anatomy/` (9 files, each SHA-256 in the fixture provenance); execution notes `docs/live-census-r6-r8/NOTES.md` | state | Same independence as R6: pure reads, sanitised by construction (AD attribute names + populated/kind booleans only; relative SYSVOL paths + byte sizes; GPT.INI copies carry `[General]`+`Version` only, displayName stripped at capture); the directory itself is the oracle | 2026-09-02 (R8); committed and hash-verified 2026-09-06 |
| W7: GPMC (Server 2025) authors a WMI filter as one `msWMI-Som` object under `CN=SOM,CN=WMIPolicy,CN=System` with `msWMI-ID` a braced GUID (`{7218E9CF-…}`), and **`msWMI-Parm2` is NOT the bare `namespace;query` pair — it is `1;3;10;35;WQL;root\CIMv2;SELECT * FROM Win32_OperatingSystem;`**: a semicolon-delimited list with a `1;3;10;35;WQL;` prefix and a trailing semicolon. The prefix constants' semantics stay uninterpreted (measured once, one WQL query); variation by query language or locale is unmeasured. The dialog's commit button is **Save**, not OK (buttons: Add/Remove/Edit + Save/Cancel), and the scope tree is keyboard-navigable but UIA-invisible — `Tree:'Console Embedded Scope'` enumerates no children, and the results-pane rows are not ListItems either | `wmi_filter` observer (guest transports raw SOM-container attribute values over PSDirect; controller-side fact tree); record `docs/estate-window-7/records/w7-record.json`; dialog/tree facts from the run journals + six screenshots `docs/estate-window-7/captures/` | state | Actuation through the GPMC main console (context-menu keyboard route: Shift+F10, DOWN, ENTER in one helper invocation); observation over PSDirect raw attribute transport; envelope evaluated controller-side — three independent channels | 2026-09-15 (revision 1, `verified`, envelope satisfied 10 clauses, 7/7 deltas covered, cleanup `removed=…`) |
| W10: certificate-template facts, measured on Server 2025 against the modern schema. **Template validity is ONE attribute, `pKIExpirationPeriod` — an 8-byte little-endian signed FILETIME interval stored negative — and there is no units attribute anywhere: `msPKI-Validity-Period` / `msPKI-Validity-PeriodUnits` exist in no Server 2025 schema, and asking for them fails the whole query.** The console's years/months/weeks/days combo renders that single duration: a built-in 1-year template decodes to exactly 365 days, and the console asked for **3 Years** wrote 946080000000000 hundred-ns = 1095 days = 3 x 365. Duplicating a V1 template (`Computer`, whose CN is `Machine`) opens `Properties of New Template` directly — **no intermediate schema-version dialog** — on the **Compatibility** page, and produces a schema-version-4 object. The property sheet's tab strip is an unnamed `SysTabControl32` (automation id 12320) with **no tab items in the UIA tree**, **Ctrl+Tab does not change the page**, and every edit is named by its CONTENT (`Copy of Computer`, `1`, `years`), never by its label; the page pane IS renamed to the selected tab, which is what makes a page switch structurally verifiable. The console auto-fills the template name (CN) from the display name until that field is hand-edited. | `certtmpl` observer (guest transports raw Certificate Templates container attributes over PSDirect; controller-side fact tree recomputes every count and digest and refuses disagreement); record `docs/estate-window-10/records/w10-r2-record.json`; dialog facts from the run journals, live UIA dumps and six screenshots `docs/estate-window-10/captures/` | state | Actuation through certtmpl.msc in the console session (helper UIA/keyboard); observation over PSDirect raw attribute transport; envelope evaluated controller-side. Strict absence after cleanup was additionally confirmed by a SEPARATE directory query that shares no code path with the driver's own cleanup re-query: 33 objects, no `zz-*` template, no orphaned `Copy of ...` | 2026-09-20 (revision 1, `verified`, envelope satisfied 13 clauses, 11/11 deltas covered, converged 2 polls / 2 reproductions, cleanup `removed=zz-wcd-certtmpl-01`) |
| W11: certificate-manager restriction facts, measured on Server 2025 against a REMOTE enterprise CA. **`CA\OfficerRights` does not exist until a console creates it** — `certutil -getreg` answers `0x80070002` and the configuration key's value-name list does not contain it — and the console wrote **188 bytes of `REG_BINARY`** (sha256 `e4f0ec16…`) as the 51st value on the key, the 50 pre-existing ones unchanged. **The CA adopts it immediately and caches nothing**: in the same observation that saw the registry value appear, `certutil -getreg CA\OfficerRights` moved from `0x80070002` to rc 0 with **4 decoded rows** — and after cleanup deleted the value, it returned to failing in the same observation that saw the value-name list lose it, so **no CertSvc restart is involved in either direction** and cleanup's `residual` is empty. Surface facts: the snap-in has **no command-line target** (four forms measured — bare, positional, `/computer=`, `/computer ` — all raise the local-CA modal with the frame still titled `Certification Authority (Local)`), so targeting is the in-console Retarget gesture and the frame title carries the target as its own oracle; **the CA's scope-tree node offers neither Properties nor Refresh while its results-pane row offers both**, and the Action menu agrees, so the results-pane twin is the only path to the property sheet; the property sheet's tab strip is **three rows and reorders on selection** (the selected tab's row moves to the bottom), so page-pane offsets hold for one switch per opening; **menus survive the next helper invocation when dumped by window class (`#32768`) but their items are UIA-invisible**, and **mnemonics do not take at all** — injected as a VK chord or as `WM_CHAR`, neither selected `Properties`, while the arrow walk did. **The Certificate Managers page does not add managers**: its list is rendered from the CA's security descriptor and labelled "(configured on the Security tab)", and the three principals it showed are exactly the `Allow Certificate Manager` grants `certutil -getreg CA\Security` decodes — so the standard Windows object picker, this surface's most-feared control, appears only on the Permissions (subject) path that revision 1 never opens | `officerrights` observer (guest reads the CA guest's `CertSvc\Configuration` key REMOTELY from the console guest over `OpenRemoteBaseKey`, and asks the CA's own RPC surface for the same value through `certutil -getreg`; controller-side fact tree refuses a disagreement between the two, and refuses an observation whose reported CA does not match the plan's argument -- which catches a garbled or misrouted read; a plan that itself names the wrong CA is caught by the sheet's targeted-frame title, not by the observer); record `docs/estate-window-11/records/w11-r2-record.json`; surface facts from the run journals, live UIA dumps and nine screenshots `docs/estate-window-11/captures/` | state | Actuation through certsrv.msc in the console session on LabMS01 (helper UIA/keyboard); observation over PSDirect from that guest to LabCA01's registry, plus certutil's independent RPC to the CA; envelope evaluated controller-side. **The actuation guest is not the mutation target** — a first for this repo — so the fact set names both machines (`certsrv.ca.host` = the CA read, `certsrv.ca.observed_from` = the console that read it) and the fact tree refuses a read whose reported CA does not match the plan's argument (a garbled or misrouted read; a plan that itself names the wrong CA is refused by the sheet's targeted-frame title, which interpolates the plan's CA host). The forbid scopes were read through a third channel again: the published-template list comes from AD, not from the CA | 2026-09-21 (revision 1, `verified`, envelope satisfied 15 clauses, 10/10 deltas covered, converged 2 polls / 2 reproductions, cleanup `removed=True` with absence confirmed through both read channels) |
| W12 (composition): the WEL-hosted lane runs the SAME qualified certtmpl bytes to `verified` — WEL's scenario runner (mvmcc02) drove `interactive_transaction` for `certtmpl.duplicate_template` (digest `a9169bf3…`, pinned in WEL's committed example): record state `verified`, 13 clauses satisfied, 11/11 deltas covered, converged 2 polls / 2 reproductions, cleanup `removed=zz-welab-certtmpl-01` with strict absence, and the scenario's `revert_environment` restored the member to `domain-joined`. Composition facts measured en route: WEL's `wait_ready` hardcodes the `guest_bootstrap` identity name (and a backend cannot bind one credential capability to two identities), WCD's record-v2 binding refuses a plan identity that differs from the estate's `identity_role` (so the vocabularies must be converged per checkout), a revert-based lane resets the MEMBER clock to checkpoint era while bring-up's clock repair is DC-scoped, and `acb`/Vault mediation for lab credentials was unavailable (AppRole login dead), so the run used the interim conventions with an operator-minted checkout receipt, disclosed in the window NOTES | record `docs/estate-window-12/records/w12-r1-record.json`; operator artifacts `local/run-certtmpl-1-live*` on mvmcc02; `docs/estate-window-12/NOTES.md` | state | The composition seam itself is the subject: the WEL journal + runner drove the driver subprocess (plan on stdin, record on stdout); the envelope was evaluated controller-side inside the driver; the directory was then re-checked through an independent PSDirect channel (33 templates, 0 `zz-*`, target absent) | 2026-09-22 (window 12, one run, `verified`) |
| W13 (composition, live proofs): the window-12 code corrections hold live, one run, no seam surprises — the same qualified certtmpl bytes (digest `a9169bf3…`, `uia_digest 9193a3df…` third window running) re-verified through the WEL-hosted lane re-pointed to a **domain_operator-only** backend: `wait_ready` answered in 2.6 s under the sole declared identity (WEL PR #36's config-side identity resolution; the pre-fix code refuses with no `guest_bootstrap` declared), record state `verified`, 13 clauses, 11/11 deltas, strict cleanup, and the estate now carries **one role label end to end** (backend, lane, and both WCD checkouts all `domain_operator` — the per-checkout convergence wart retired). `estate_bringup` then proved WCD PR #20's member-clock step live over the reverted member (checkpoint-era skew −404,644 s repaired to 2 s by the tz-safe Set-Date alone, locator green, first-try CAD re-logon) and the closing canary ran 8/8 exit 0 against the re-pointed estate file | record `docs/estate-window-13/records/w13-r1-record.json`; operator artifacts `local/run-w13-r1-live*` on mvmcc02; `docs/estate-window-13/NOTES.md` | state | Same composition shape as W12 (WEL journal + runner → driver subprocess, plan on stdin / record on stdout); the directory re-check moved to LabCL01 — PSDirect into a guest the lane never touched plus explicit-credential LDAP to the DC — removing the member from the verification path entirely | 2026-09-25 (window 13, one run, `verified`) |
